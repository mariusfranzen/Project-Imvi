<UserControl x:Class="Project_Imvi.UserControls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_Imvi.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Button Content="Previous" Grid.Column="0" Grid.Row="0" Command="{Binding PreviousCommand}"/>
        <Button Content="Slideshow" Grid.Column="1" Grid.Row="0" Command="{Binding SlideshowCommand}"/>
        <Button Content="Next" Grid.Column="2" Grid.Row="0" Command="{Binding NextCommand}"/>

    </Grid>
</UserControl>
